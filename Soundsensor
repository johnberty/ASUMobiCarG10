int soundsensor = 2;
int led = 3 ;
void setup() {
pinMode(soundsensor, INPUT);
pinMode(led, OUTPUT);
}
void loop() {
if(digitalRead (soundsensor)==1)
{
  digitalWrite (led,1);
  }
  else
  {
    digitalWrite (led,0);
    }
}
